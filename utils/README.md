# lab-drawer Utilities

共用工具函式，供 lab-drawer collection 內的 skills 使用。

## 目前狀態

尚未有共用工具。未來若多個 skills 需要相同的工具函式（如日期格式化、金額處理等），可以放在此目錄。

## 新增工具規範

- 每個工具獨立一個檔案
- 使用 JavaScript（Node.js compatible）
- 每個檔案開頭加註解說明用途和用法
- 在 `index.js` 統一 export
